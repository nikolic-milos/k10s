# Lab fixture only. Address only; no Vault install, no VSO.
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultConnection
metadata:
  name: k10s-hplane-vault
  namespace: k10s-hplane
  labels:
    k10s.dev/hplane-fixture: "true"
  annotations:
    k10s.lab/tokenSecret: PLANTED-live-canary
spec:
  address: http://127.0.0.1:8200
  skipTLSVerify: true
