# Lab fixture only. References only; no secret values, no ESO controller.
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: k10s-hplane-store
  namespace: k10s-hplane
  labels:
    k10s.dev/hplane-fixture: "true"
  annotations:
    k10s.lab/canary: PLANTED-live-canary
spec:
  refreshInterval: 3600
  provider:
    fake:
      data: []
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: k10s-hplane-external
  namespace: k10s-hplane
  labels:
    k10s.dev/hplane-fixture: "true"
  annotations:
    k10s.lab/canary: PLANTED-live-canary
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: k10s-hplane-store
    kind: SecretStore
  target:
    name: k10s-hplane-eso-target
    creationPolicy: Orphan
  data:
  - secretKey: placeholder
    remoteRef:
      key: k10s-hplane-remote
      property: placeholder
