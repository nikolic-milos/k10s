# Lab fixture only. No Cilium agent; this does not change CNI or kube-proxy.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: k10s-hplane-cnp
  namespace: k10s-hplane
  labels:
    k10s.dev/hplane-fixture: "true"
  annotations:
    k10s.lab/canary: PLANTED-live-canary
spec:
  endpointSelector:
    matchLabels:
      app: k10s-hplane
  ingress:
  - {}
